#pragma once

// buffer limit for keyboard and mouse input
const int kInputBufferLimit = 16;

// final render size
const int kRenderWidth = 1280, kRenderHeight = 720;

const float kScreenRatio = static_cast<float>(kRenderHeight) / static_cast<float>(kRenderWidth);

const double kPI = 3.1415926;
